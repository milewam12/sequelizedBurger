<div class="container">

<div class="text-center">
        
    <iframe src="https://giphy.com/embed/5dXzOahWX5wB2" width="300" height="300" frameBorder="0" class="giphy-embed" allowFullScreen></iframe>
</div>

  <div class="row">
    <div class="col">
      {{#each burgers}}
{{!-- <p>{{this.burger_name}} {{this.devoured}}</p> --}}
{{#unless this.devoured}}

<p>{{this.burger_name}}</p>
<form action="bugers/update/{{this.id}}?_method=PUT" method="POST">
<input type="hidden" name="devoured" value="false">
<button type="submit" class="btn" data-id="{{id}}" data-donedevoured="{{devoured}}">{{#unless devoured}}Devour it!{{else}}Too late!{{/unless}}</button>
</form>

{{/unless}}
{{/each}}
  </div>


    <div class="col">
     {{#each burgers}}
{{#if this.devoured}}


    <form action="bugers/insertone/{{this.id}}?_method=POST" method="POST">
<input type="text" class="form-control" placeholder={{this.burger_name}}>


{{/if}}
{{/each}}
    </div>
  </div>
{{!-- end of the row --}}

 <div class="text-center">
    <h1>Eat-Da-Burger!</h1>
            <form class="create-form" >

                        <div class="form-group">
                            <input type="text" id="bu" name="name" class="form-control text-center" placeholder="Your Burger Goes Here!" >
                            <br>
                        <label for="ca">Enter a burger you would like to eat. Click the submit button</label>
                        
                        </div>
                    
                        <button type="submit" class="btn btn-danger btn-lg">Submit</button>
                    </form>

        </div>

</div>



